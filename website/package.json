{
  "private": true,
  "version": "0.0.0",
  "name": "website",
  "scripts": {
    "dev": "saber",
    "generate": "saber build && yarn workspace saber build:typedoc"
  },
  "devDependencies": {
    "speed-measure-webpack-plugin": "^1.3.1",
    "@bundle-analyzer/webpack-plugin": "^0.5.0"
  }
}
